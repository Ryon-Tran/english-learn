/**
 * Linh vật thương hiệu — bạch tuộc: gợi ý “nhiều việc cùng lúc” (4 kỹ năng tiếng Anh).
 * Ảnh PNG: /public/mascot-tako.png — component `MascotAvatar`.
 * SVG dự phòng / icon nhỏ: `MascotOctopusMark`.
 */
export const mascot = {
  /** Tên gọi ngắn (hiển thị UI) */
  name: "Tako",
  /** Một câu giải thích cho user */
  tagline: "Một nhà bốn tay — luyện đủ Reading, Listening, Writing, Speaking cho mọi loại đề bạn chọn.",
} as const;
